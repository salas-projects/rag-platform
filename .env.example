# Copy to .env and fill in real values. .env is gitignored — never commit real secrets.

# Generation (Claude) — pay-as-you-go API balance, separate from the Pro coding subscription.
ANTHROPIC_API_KEY=

# Embeddings (Voyage AI) — see adr/0002-embeddings-voyage-ai.md
VOYAGE_API_KEY=

# Vector store (Qdrant, self-hosted via infra/docker-compose.yml)
QDRANT_URL=http://localhost:6333

# Slack app (Bolt SDK, Socket Mode) — built in Phase 3, see adr/0003-distribution-slack-first.md
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
