version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage

# Airflow services are added in Phase 4 (orchestration) — not needed
# for the initial core pipeline build. See /docs/PROJECT_SCOPE.md.
